//Unary operator
//Increment  opeartor
let a = 20
    // console.log(a++); //show that number before the increment (post increment)
    // console.log(a); // now decrement already done so it show21
console.log(++a); //value already incremented so it will be print 21(pre-increment)




//Decrement operator
let b = 20
    // console.log(b--); //show that number before the increment (post decrement)
    // console.log(b); // now decrement already done so it show21
console.log(--b); //value already decremented so it will be print 19(pre-decrement)
// console.log(b);